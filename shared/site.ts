/**
 * Canonical production origin — used to build absolute canonical URLs
 * (app/layouts/default.vue) and the sitemap (server/routes/sitemap.xml.get.ts).
 * No trailing slash.
 */
export const SITE_URL = 'https://eaaa11yhub.eu'

export const SITE_NAME = 'EAA A11y Hub'
